[
  "CAS recovery allows only one competing process epoch, not a stale process takeover",
  "a migrated v2 root without delegation consent retains main routing but exposes no child tool",
  "a native picker change automatically withdraws v2 control before the next request",
  "a native picker change while creating a child revokes its first fetch and restores ordinary parent control",
  "a publication failure cleans the owned child before any child request",
  "accepts only actually observed range hashes and exactly one final submission",
  "authorizes an explicitly selected gpt-6-luna child route without widening the default start",
  "authorizes an explicitly selected gpt-6-sol child route without widening the default start",
  "an unavailable installed model is rejected without fallback, dispatch or a new debit",
  "an unobserved but correct file hash is not an observed citation",
  "artifacts reject symlink directories, hardlinks, public modes, traversal and oversize",
  "artifacts survive new store instances, are private and detect tampering",
  "binds exact child identity/route and rejects replayed attempts without a second permit",
  "blocks downgrade if ordinary parent delivery evidence disappears despite a recorded ledger flag",
  "blocks missing archive provenance and refuses wrong owner/key/CAS",
  "cleanup failure blocks new root dispatch and is not promoted to success",
  "create failure yields no child fetch, preserves receipt and does not retry",
  "creates an unpublished-tool-scoped child, runs it through the real loop, and disposes it",
  "deadline and disposal cancel owned transport without refund or subsequent fetch",
  "detaches manifest listings and snapshot reader inputs from caller mutation",
  "detects malformed snapshot hash and duplicate IDs",
  "downgrades only manual v2, archives exact provenance, and preserves counters",
  "executes only explicit evidence consent, then archives/downgrades/remigrates without replay or budget reset",
  "executes read/submit through real host and shared governor with durable normal delivery (none)",
  "executes read/submit through real host and shared governor with durable normal delivery (zstd)",
  "explicit same-owner idle resume reattaches tools without resetting budget or auto-respawning children",
  "fails closed for non-live or foreign parents and setup checks",
  "fences duplicate operations, changed digests, stale UI and another owner before changing consent",
  "hands the main task to explicitly authorized gpt-6-luna without changing the default start",
  "hands the main task to explicitly authorized gpt-6-sol without changing the default start",
  "is idempotent for the same operation and rejects changed operation digest",
  "keeps legacy/off behavior, explicitly migrates without child authority, and preserves the root budget",
  "keeps v2 authority unchanged if interrupted after archive persistence but before root replacement",
  "lost reply reconciliation uses the original durable call/result, without new provider work",
  "main route changes preserve budget and fence stale queued attempts, selection and revision",
  "manual takeover discards a queued route and restores the ordinary picker without resetting spent budget",
  "migrates v1 auto to interrupted v2 without enabling or resetting counters",
  "migration preserves interrupted state",
  "migration preserves limit state",
  "migration preserves manual state",
  "migration preserves stopped state",
  "ordinary parent Stop cancels child ownership and is consumed as durable revocation",
  "preserves current higher counters and retained child replay records after old v1 receipts are evicted",
  "reads v1 unchanged and migrates only explicitly, preserving root authority/history/counts",
  "recovers prepared without replay or refund; old epoch loses all mutation authority",
  "recovers running without replay or refund; old epoch loses all mutation authority",
  "recovers settling without replay or refund; old epoch loses all mutation authority",
  "recovers succeeded without replay or refund; old epoch loses all mutation authority",
  "refuses a child before preparation when the parent call has no durability listener",
  "refuses byte capacity before admission while leaving room to finish the last accepted run",
  "refuses wrong owner, root key, stale revision and missing state without creating new grants",
  "rejects a live child in assertNoChildren until its handle is disposed",
  "rejects a symlink file, directory component and root",
  "rejects empty evidence units and changed duplicate route, sources or snapshot even with the same digest",
  "rejects empty references, oversized results and extra fields",
  "rejects empty, duplicate, too many, and out-of-scope source choices",
  "rejects excluded or noncanonical path ../outside.txt before opening it",
  "rejects excluded or noncanonical path .env before opening it",
  "rejects excluded or noncanonical path .git/HEAD before opening it",
  "rejects excluded or noncanonical path /absolute.txt before opening it",
  "rejects excluded or noncanonical path auth.json before opening it",
  "rejects excluded or noncanonical path config.json before opening it",
  "rejects excluded or noncanonical path credentials.txt before opening it",
  "rejects excluded or noncanonical path private.key before opening it",
  "rejects excluded or noncanonical path sub/../notes.txt before opening it",
  "rejects excluded or noncanonical path sub\\notes.txt before opening it",
  "rejects expanded or malformed consent {\"approved\":true}",
  "rejects expanded or malformed consent {\"maxRequests\":0}",
  "rejects expanded or malformed consent {\"maxRequests\":7}",
  "rejects expanded or malformed consent {\"routes\":[{\"model\":\"gpt-5.6-sol\",\"effort\":\"medium\"},{\"effort\":\"medium\",\"model\":\"gpt-5.6-sol\"}]}",
  "rejects expanded or malformed consent {\"routes\":[{\"model\":\"gpt-6-astra\",\"effort\":\"max\"}]}",
  "rejects expanded or malformed consent {\"sourceIds\":[\"approved-source-0001\",\"approved-source-0001\"]}",
  "rejects expanded or malformed consent {\"sourceManifest\":\"forged\"}",
  "rejects expanded or malformed consent {\"timeoutMs\":90001}",
  "rejects expanded or malformed consent {\"timeoutMs\":999}",
  "rejects expanded or undisclosed scope {\"disclose\":false}",
  "rejects expanded or undisclosed scope {\"files\":[\"../outside.txt\"]}",
  "rejects expanded or undisclosed scope {\"files\":[\".env\"]}",
  "rejects expanded or undisclosed scope {\"maxChildRequests\":7}",
  "rejects expanded or undisclosed scope {\"routes\":[{\"model\":\"gpt-6-astra\",\"effort\":\"max\"}]}",
  "rejects expanded or undisclosed scope {\"routes\":[{\"model\":\"gpt-6-luna\",\"effort\":\"max\"}]}",
  "rejects expanded or undisclosed scope {\"routes\":[{\"model\":\"gpt-6-sol\",\"effort\":\"max\"}]}",
  "rejects hardlinks and a file replaced with symlink after approval",
  "rejects invalid UTF8/binary bytes",
  "rejects invalid UTF8/binary bytes",
  "rejects invalid UTF8/binary bytes",
  "rejects known private-key paths and PEM payloads even under an ordinary text filename",
  "rejects out of bounds, arbitrary path and oversized line requests",
  "rejects out-of-scope model/source and forged parent call without spawning",
  "rejects oversized files and total snapshots without truncation",
  "rejects an old grant route for gpt-6-luna even when the live catalog advertises it",
  "rejects an old grant route for gpt-6-sol even when the live catalog advertises it",
  "rejects unknown delivery and missing evidence before creating a representable downgrade",
  "requires consent and exact fencing, supports different authorized routes, and isolates roots",
  "requires downgrade guard and keeps document unchanged when artifact validation fails",
  "requires idle consent but stop/manual can cancel an active child and preserve debits",
  "requires verified cleanup and separately reconciled delivery; neither is inferred from success",
  "restores the exact approved manifest from a digest-bound artifact, without issuing new source IDs",
  "resume never overrides a manual picker change made while interrupted",
  "retains a parent request and stops a child at the shared budget floor",
  "retains all 32 admitted cancelled/failed run receipts and rejects the 33rd before any work",
  "retains full receipts while allowing stop/manual/revocation/downgrade even without the model catalog",
  "revocation after host creation but before ledger publication never dispatches the child",
  "revocation during a pending child request prevents later fetch and drains ownership (manual)",
  "revocation during a pending child request prevents later fetch and drains ownership (stopped)",
  "revocation to manual fences publication/reservation/late success",
  "revocation to stopped fences publication/reservation/late success",
  "round-trips manual v1 through its retained archive and rejects archive tampering",
  "selects a second authorized child route instead of hard-coding Luna/Low",
  "serializes two independent admissions to one active child",
  "shares the root lock for child/auxiliary debits and enforces child cap and parent floor",
  "snapshots queued arguments and rejects duplicate call conflicts without spawning twice",
  "stop after preparation prevents publication and an expired deadline prevents reservation",
  "strictly rejects corrupt bad-source without replacing the valid file",
  "strictly rejects corrupt duplicate-call without replacing the valid file",
  "strictly rejects corrupt extra-root without replacing the valid file",
  "strictly rejects corrupt extra-run without replacing the valid file",
  "strictly rejects corrupt fake-success without replacing the valid file",
  "strictly rejects corrupt lost-debit without replacing the valid file",
  "strictly rejects corrupt oversize-id without replacing the valid file",
  "strictly rejects corrupt unknown-version without replacing the valid file",
  "strictly rejects corrupt unsafe-number without replacing the valid file",
  "the actual child deadline cancels a hung request and preserves the spent debit",
  "tool registration failure during explicit resume withdraws all authority and persists stopped",
  "unknown live descendants and auxiliary child work cannot borrow a root scope",
  "unloads/resumes the persisted parent and reconciles a lost reply without respawning (none)",
  "unloads/resumes the persisted parent and reconciles a lost reply without respawning (zstd)",
  "unobserved findings fail and parent receives a bounded failed outcome",
  "keeps signed-cookie activation closed and preserves existing delegation safety exits and ownership",
  "uses immutable snapshots and host IDs, not child-supplied paths",
  "v2 compaction is counted and handoff restores the original journal (native=false)",
  "v2 compaction is counted and handoff restores the original journal (native=true)",
  "v2 main model changes on the next real request within the same grant and shared counter",
  "v2 main-model changes cannot run while a child is unresolved",
  "v2 title-purpose and direct auxiliary calls share the root counter without double debit",
  "keeps standalone streams and auxiliary work ordinary without optional task services",
  "rejects live task control and dispatch when the session service is missing"
]
